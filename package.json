{
  "name": "catapult-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Scripts to help debug Catapult",
  "main": "dist/src/index.js",
  "bin": {
    "catapult-mongo-dump": "dist/bin/catapult-mongo-dump.js"
  },
  "scripts": {
    "build": "babel src -d dist/src && babel bin -d dist/bin",
    "clean": "rm -rf dist",
    "lint": "eslint src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "mongodb": "^3.5.3",
    "yargs": "^15.1.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.4",
    "@babel/polyfill": "^7.8.3",
    "@babel/preset-env": "^7.8.4",
    "@babel/register": "^7.8.3",
    "babel-eslint": "^10.0.3",
    "eslint": "^6.8.0"
  }
}
